import { UPDATE_TEXT } from './actions';

const defaultState = {};

export default function uiActions(state = defaultState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
